# Copy this file to .env for local or Docker use. Never commit .env.
APP_ENV=development
SECRET_KEY=
HOST=127.0.0.1
PORT=8000
CORS_ORIGINS=http://127.0.0.1:8000,http://localhost:8000
DATABASE_URL=sqlite:///./ai_parking.db

# Used only when running `python -m backend.init_data`.
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.invalid
ADMIN_PASSWORD=
